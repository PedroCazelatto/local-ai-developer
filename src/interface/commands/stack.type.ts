// The stacks /new-project can scaffold. A closed union owned by no function: known-stacks.ts lists
// them, is-known-stack.ts narrows onto it, stack-template.ts keys its table by it and
// readme-placeholder.ts prints it. Add stacks ON DEMAND, not preemptively (CLAUDE.md).

export type Stack = 'node' | 'python';
