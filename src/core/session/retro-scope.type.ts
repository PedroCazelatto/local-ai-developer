// Where a Retro's fix belongs. Decided by the RESOLVED edited-file path -- the orchestrator's path
// guard -- and never by the model's self-declared scope, which is advisory only.

/** Where the fix belongs — decided by the resolved edited-file path, not the model's claim. */
export type RetroScope = 'systemic' | 'task-specific';
