// The Retro scopes as a runtime list, for validating what the model submitted. A VALUE, so it is a
// plain constant module rather than a .type.ts.

import type { RetroScope } from './retro-scope.type.js';

export const RETRO_SCOPES: readonly RetroScope[] = ['systemic', 'task-specific'];
