// Which read-only git question git_inspect is being asked. A closed union, spoken by the tool, the
// guard that narrows an untyped argument to it and the list that names the legal values back to the
// model — folder vocabulary rather than any one function's type.

/** The three read-only history questions git_inspect answers. */
export type InspectWhat = 'diff' | 'log' | 'show';
