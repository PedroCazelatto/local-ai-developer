// Which shelf operation git_stash is being asked for. A closed union, spoken by the tool, the guard
// that narrows an untyped argument to it and the list that names the legal values back to the model.

/** The four shelf operations git_stash performs. */
export type StashAction = 'save' | 'list' | 'pop' | 'drop';
