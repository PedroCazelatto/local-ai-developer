// Which branch operation git_branch is being asked for. A closed union, spoken by the tool, the guard
// that narrows an untyped argument to it and the list that names the legal values back to the model.

/** The three branch operations git_branch performs — nothing here deletes a branch. */
export type BranchAction = 'create' | 'switch' | 'list';
