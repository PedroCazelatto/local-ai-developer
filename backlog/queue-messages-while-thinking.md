# Queue messages sent while the model is thinking

**Category:** Terminal UI / rendering

Let the user submit more messages during a turn; queue them and run them in order after the response
finishes. While a message is queued, pressing ↑ (arrow up) un-queues the most recent one and refills
the input for editing.
