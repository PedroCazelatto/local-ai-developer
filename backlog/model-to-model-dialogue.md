# Sharpen the model's thinking via model-to-model dialogue

**Category:** Model behavior / instructions

Add prompts/mechanism for one model context to talk to another and discuss ideas — a
deliberation/debate loop (e.g. a second spawned window that challenges and refines the first's
reasoning) so the model pressure-tests an idea before committing to an answer. (One Ollama model,
two context windows — per the core mental model.)
