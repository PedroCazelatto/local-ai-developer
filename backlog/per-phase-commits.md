# Per-phase commits

**Category:** Model behavior / instructions

Give every phase the ability to commit its changes. A **subagent** must write the commit messages.
Each commit must be **as small as possible without breaking the application**.
